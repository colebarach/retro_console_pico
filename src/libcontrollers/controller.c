// Header
#include "controller.h"

// Functions ------------------------------------------------------------------------------------------------------------------

void controllerRead(controller_t* controller)
{
    controller->read(controller);
}
